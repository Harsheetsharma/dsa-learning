
#include <iostream>

using namespace std;

int main()
{
    // int array[] = {5, 3, 2, 1};
    // int size = sizeof(array) / sizeof(array[0]);
    // int *firstPointer = array;
    // int *secondPointer = &array[1];
    // int *lastPointer = &array[size - 1];
    // int sum;
    // int prod;
    // for (int i = 0; i <= size; i++)
    // {
    //     sum = firstPointer[i] + secondPointer[i];
    //     prod = firstPointer[i] * secondPointer[i];

    //     if (secondPointer[i] == lastPointer[i])
    //     {
    //         break;
    //     }
    //     firstPointer[i]++;
    //     secondPointer[i]++;
    // }
    // cout << sum << endl;
    // cout << prod;
    return 0;
}

// Homework
// WAF to calculate sum & product of all numbers in an array.
// WAF to swap the max & min number of an array.
// WAF to print all the unique values in an array.
// WAF to print intersection of 2 arrays.